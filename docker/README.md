# Docker Support Files

This folder is reserved for additional Docker support assets as the project grows.

Current runtime configuration is handled by:
- `../Dockerfile` for the Express backend
- `../client/Dockerfile` for the Vite frontend build
- `../docker-compose.yml` for local orchestration
